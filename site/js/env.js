// Deployment endpoints. The Worker URL is the only environment-specific
// value in the shipped site; update it when deploying under your own domain.
export const WORKER_URL = 'https://signage-api.rvc.tech';
